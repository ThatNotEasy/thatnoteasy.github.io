# Example: ~/.continue/mcpServers/playwright-mcp.yaml
name: Playwright MCP Server
version: 0.0.1
schema: v1
mcpServers:
  - name: Browser search
    command: npx
    args:
      - "@playwright/mcp@latest"
    env: {}